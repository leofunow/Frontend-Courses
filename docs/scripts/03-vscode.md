Что открыть заранее: слайды, текст урока, VS Code на своём компьютере с открытой папкой любого проекта. Шаблон домашних заданий в виде архива — отправьте ученику ссылку до занятия.

Цель занятия: ученик уверенно открывает папку проекта, пользуется палитрой команд и главными сочетаниями, у него работают Biome и Live Server.

Формат: половину времени ученик повторяет всё у себя, вы смотрите его экран.


## Титульный слайд (1 мин)

**На слайде:** название урока.

**Рассказать:** сегодня настраиваем редактор, в котором пройдёт весь курс.

## Часть 1. Редактор (1 мин)

**На слайде:** разделитель первой части.

**Рассказать:** сначала — из чего состоит VS Code и как открыть проект.

## Редактор — главное рабочее место (1 мин)

**На слайде:** тезис.

**Рассказать:** программист проводит в редакторе больше времени, чем в браузере. Каждая удобная мелочь окупается.

## Интерфейс (3 мин)

**На слайде:** скриншот VS Code с четырьмя зонами.

**Показать вживую:** ученик открывает VS Code и находит у себя все четыре зоны. ⌘B прячет и показывает боковую панель.

## Открываем папку, а не файл (4 мин)

**На слайде:** кнопка Open Folder и три способа открыть папку.

**Показать вживую:** открыть папку тремя способами: ⌘O, перетаскиванием на Dock и `code .` из Терминала. На вопрос о доверии — Yes, I trust the authors.

## Файлы и папки (3 мин)

**На слайде:** дерево учебного проекта.

**Показать вживую:** создать файл `css/style.css` одной строкой — папка появилась сама.

## Проводник (2 мин)

**На слайде:** четыре карточки: новый файл, переименовать, удалить, точка несохранённого файла.

**Рассказать:** точка ● на вкладке — самая частая причина «правки не видны в браузере».

## Часть 2. Клавиши (1 мин)

**На слайде:** разделитель второй части.

**Рассказать:** мышью долго. Несколько сочетаний ускоряют работу в разы.

## Палитра команд (4 мин)

**На слайде:** палитра с командой Format Document.

**Показать вживую:** ⇧⌘P → `format` → Enter. Справа от команды — её сочетание. ⌘P → `ind` → открыть index.html.

## Горячие клавиши (4 мин)

**На слайде:** пять главных сочетаний.

**Показать вживую:** ученик повторяет каждое на любом файле: ⌥↑ двигает строку, ⇧⌥↓ дублирует.

## Несколько курсоров: ⌘D (3 мин)

**На слайде:** список до и после ⌘D.

**Показать вживую:** выделить `item`, два раза ⌘D, напечатать `card`. Esc — снова один курсор.

## Быстро ходить по коду (3 мин)

**На слайде:** пять сочетаний навигации.

**Рассказать:** те же ⌘← и ⌥← работают в браузере, Заметках и Терминале — выучил один раз, пользуешься везде.

## Три сочетания на первую неделю (2 мин)

**На слайде:** три карточки: ⌘P, ⌘D, ⌘\\.

**Показать вживую:** ⌘\\ — открыть рядом HTML и CSS, как будет в уроке про стили.

## Часть 3. Emmet и расширения (1 мин)

**На слайде:** разделитель третьей части.

**Рассказать:** дальше — ускорители: Emmet и расширения.

## Emmet (3 мин)

**На слайде:** подсказка Emmet после `!`.

**Показать вживую:** новый файл `index.html` → `!` → Tab. Каркас готов, `lang` меняем на `ru`.

## Сокращения Emmet (4 мин)

**На слайде:** таблица сокращений.

**Показать вживую:** ученик набирает каждое сокращение и смотрит результат.

## Одна строка — целый список (2 мин)

**На слайде:** `ul>li.item$*3` и результат.

**Рассказать:** `$` — номер по порядку. Сложные сокращения не нужны: хватает `>`, `*`, `.` и `{}`.

## Расширения (4 мин)

**На слайде:** Live Server и Biome в панели расширений.

**Показать вживую:** ученик ставит оба расширения. Проверяем автора: biomejs и Ritwick Dey.

## Настройки (2 мин)

**На слайде:** поиск настройки Format On Save.

**Показать вживую:** ⌘, → в поиске `format on save` → включить.

## Настройки текстом (3 мин)

**На слайде:** JSON с пятью настройками.

**Показать вживую:** ⇧⌘P → `Open User Settings (JSON)` → вставить. Проверить `linkedEditing`: поменять `h1` в открывающем теге — поменялся и закрывающий.

## Встроенный терминал (2 мин)

**На слайде:** встроенный терминал в папке проекта.

**Показать вживую:** ⌃` → `ls` — видны файлы проекта.

## Часть 4. Проверка кода (1 мин)

**На слайде:** разделитель четвёртой части.

**Рассказать:** последняя часть — инструменты, которые ловят ошибки.

## Папка для домашки (6 мин)

**На слайде:** три шага: распаковать, открыть, `npm install`.

**Показать вживую:** ученик распаковывает шаблон в `~/dev/homework`, ставит рекомендованные расширения и выполняет `npm install`.

## Линтер Biome (4 мин)

**На слайде:** схема: код → ⌘S → Biome → ровный код.

**Показать вживую:** стереть `alt` у картинки → волнистая линия → навести → ⇧⌘M. Сбить отступы → ⌘S — выровнялись.

## Как читать сообщение линтера (3 мин)

**На слайде:** вывод `npx biome check`.

**Рассказать:** каждое сообщение устроено одинаково: файл, строка, колонка, правило, что не так.

**Спросить:** «Где ошибка?» Ученик называет файл, строку и колонку, потом ⌃G → номер строки.

## Live Server (3 мин)

**На слайде:** схема Live Server.

**Показать вживую:** Go Live → поменять текст → ⌘S → страница обновилась сама.

## История файла: Timeline (3 мин)

**На слайде:** четыре шага восстановления версии.

**Показать вживую:** удалить абзац, сохранить, закрыть файл, открыть снова — ⌘Z не помогает. Timeline → прошлая версия → Restore Contents.

## Частые ошибки (2 мин)

**На слайде:** четыре карточки.

**Рассказать:** Emmet не работает — посмотрите язык файла в правом нижнем углу.

## Воркшоп: мини-проект в VS Code (18 мин)

**На слайде:** шесть шагов мини-проекта.

**Подготовить заранее:** закройте все окна VS Code. Увеличьте масштаб для экрана: <kbd>⌘</kbd>+<kbd>=</kbd> два раза. Держите открытой папку `~/dev/homework` — понадобится на шаге 6.

**Показать вживую:**
1. **Открыть папку.** Меню File → Open Folder (<kbd>⌘</kbd>+<kbd>O</kbd>) → `dev` → кнопка New Folder внизу окна → `workshop-vscode` → Open.
   - На вопрос «Do you trust the authors» → Yes, I trust the authors.
   - Покажите четыре зоны: панель действий слева, проводник, редактор, строка состояния.
2. **Файлы.** Наведите мышь на название папки в проводнике — справа появятся иконки.
   - Иконка New File → `index.html` → Enter.
   - Иконка New Folder → `css`. Правый щелчок по `css` → New File → `style.css`.
3. **Emmet.** В `index.html` наберите `!` → <kbd>Tab</kbd> — готов каркас. Поменяйте `lang="en"` на `lang="ru"`: двойной щелчок по `en`, напечатать `ru`.
   - Внутри `body`: `header>nav>a*3` → <kbd>Tab</kbd>.
   - Ниже: `main>h1{Кофейня}+ul>li.dish*4` → <kbd>Tab</kbd>.
4. **Горячие клавиши.**
   - Выделите мышью `href=""` у первой ссылки → <kbd>⌘</kbd>+<kbd>D</kbd> ещё дважды — выделены все три. Напечатайте `href="#"` — поменялись все ссылки сразу. <kbd>Esc</kbd> — снова один курсор.
   - Курсор на строке `<h1>` → <kbd>⌥</kbd>+<kbd>↑</kbd> — строка поднялась. <kbd>⇧</kbd>+<kbd>⌥</kbd>+<kbd>↓</kbd> — копия строки ниже.
   - <kbd>⌘</kbd>+<kbd>/</kbd> — закомментировать строку, ещё раз — вернуть.
   - <kbd>⌘</kbd>+<kbd>P</kbd> → наберите `sty` → Enter: открылся `style.css` без мыши.
5. **Biome.** Вернитесь в `index.html` и добавьте `<img src="logo.png">`.
   - Появится волнистая линия. Наведите — сообщение про `alt`.
   - <kbd>⇧</kbd>+<kbd>⌘</kbd>+<kbd>M</kbd> — панель Problems со всеми ошибками. Щелчок по строке переносит к ошибке.
   - Допишите `alt="Логотип"` — ошибка пропала.
   - Сломайте отступы и нажмите <kbd>⌘</kbd>+<kbd>S</kbd> — Biome выровнял код при сохранении.
6. **Live Server.** Справа внизу в строке состояния — Go Live. Щелчок → Chrome откроет `http://127.0.0.1:5500`.
   - Поставьте окна рядом, поменяйте текст `h1` → <kbd>⌘</kbd>+<kbd>S</kbd> — страница обновилась сама.
7. **Терминал внутри.** <kbd>⌃</kbd>+<kbd>`</kbd> — снизу открылся терминал уже в папке проекта. `ls` — видны наши файлы.
   - File → Open Recent → `homework`. В терминале `npx biome check 03-vscode` — так ученик проверяет домашку.
8. **Настройки.** <kbd>⌘</kbd>+<kbd>,</kbd> → в поиске `font size` → поменяйте на 16. Иконка с листком справа вверху — те же настройки в `settings.json`.

**Рассказать:** за 15 минут — страница без единого щелчка по меню. Ученику не нужно запоминать всё сразу: достаточно Emmet, <kbd>⌘</kbd>+<kbd>P</kbd>, <kbd>⌘</kbd>+<kbd>D</kbd> и Go Live.

## Домашнее задание (3 мин)

**На слайде:** пять заданий.

**Рассказать:**
- Задание 3 проверяется прямо на странице — плашка станет зелёной.
- Ответы и скриншоты — в полях под заданиями, в конце урока — отчёт.
