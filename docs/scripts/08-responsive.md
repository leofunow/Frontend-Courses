Что открыть заранее: слайды, текст урока (песочницы), VS Code с домашкой, Chrome с DevTools. Если есть телефон — сайт курса или любая страница ученика по Live Server в одной Wi-Fi-сети.

Цель занятия: ученик проверяет страницу в режиме устройств, пишет медиазапросы по принципу mobile-first, убирает фиксированные ширины и выносит цвета в переменные.


## Титульный слайд (1 мин)

**На слайде:** название урока.

**Рассказать:** до сих пор всё верстали под ноутбук. Сегодня научим страницы жить на телефоне.

## Часть 1. Один сайт — любой экран (1 мин)

**На слайде:** разделитель первой части.

**Рассказать:** сначала — как увидеть страницу глазами телефона.

## Раньше делали два сайта (2 мин)

**На слайде:** тезис про мобильные версии `m.`.

**Рассказать:** мобильные версии живут до сих пор, но это двойная работа. Адаптивная вёрстка — один HTML на все экраны.

## Один сайт, три ширины (3 мин)

**На слайде:** одна страница на телефоне, планшете и ноутбуке.

**Показать вживую:** открыть сайт курса и сузить окно браузера: боковое меню пропадает, контент перестраивается.

## meta viewport (4 мин)

**На слайде:** строка viewport и два скриншота.

**Рассказать:** телефон по умолчанию притворяется экраном 980px и уменьшает страницу. Строка viewport это отменяет.

## Режим устройств (5 мин)

**На слайде:** кнопка режима устройств в DevTools.

**Показать вживую:** ⇧⌘M на странице ученика, выбрать iPhone SE, потом Responsive и потянуть край. Удалить viewport в Elements и обновить — страница мельчает.

## Три ширины для проверки (2 мин)

**На слайде:** 375, 768, 1280 крупными числами.

**Рассказать:** эти три ширины проверяем после каждой работы. Горизонтальная прокрутка на телефоне — всегда ошибка.

## Часть 2. Медиазапросы (1 мин)

**На слайде:** разделитель второй части.

**Рассказать:** теперь — как поменять стили на нужной ширине.

## Медиазапрос (6 мин)

**На слайде:** одна колонка и две от 640px.

**Показать вживую:** песочница «Медиазапрос» — ученик меняет 300 на число больше ширины превью, блок синеет.

## Сначала телефон (4 мин)

**На слайде:** шкала брейкпоинтов 0 → 640 → 1024.

**Рассказать:** телефон — самая простая раскладка. Её проще расширять, чем урезать. Брейкпоинт ставят там, где вёрстка начинает ломаться.

## Порядок в файле (2 мин)

**На слайде:** базовые стили и два медиазапроса.

**Рассказать:** медиазапросы в конце файла, от меньшей ширины к большей. Правило ниже побеждает — как в уроке про каскад.

## Диапазон (2 мин)

**На слайде:** `and` и короткая запись с `<=`.

**Рассказать:** диапазоны нужны редко: при mobile-first хватает `min-width`. Но в чужом коде они встретятся.

## Не только ширина (3 мин)

**На слайде:** четыре медиафункции.

**Показать вживую:** в macOS включить тёмное оформление: сайт курса остаётся светлым, а GitHub меняется — он слушает `prefers-color-scheme`.

## Часть 3. Резиновые размеры (1 мин)

**На слайде:** разделитель третьей части.

**Рассказать:** меньше медиазапросов — меньше работы. Многие размеры подстраиваются сами.

## Единицы и функции (3 мин)

**На слайде:** таблица единиц.

**Показать вживую:** песочница «Заголовок, который тянется» — сузить окно, размер плавно меняется и упирается в границы.

## width или max-width (2 мин)

**На слайде:** две строки.

**Рассказать:** почти любой `width` в пикселях у контейнера должен стать `max-width`.

## min() и max() (2 мин)

**На слайде:** две функции на примерах.

**Рассказать:** `min(100% - 32px, 960px)` — это `max-width` и поля в одной строке.

## Картинки (2 мин)

**На слайде:** три строки для img.

**Показать вживую:** картинка в задании 4 до и после `max-width: 100%` в DevTools.

## object-fit (2 мин)

**На слайде:** fill, contain, cover.

**Рассказать:** object-fit нужен, только когда у картинки заданы и ширина, и высота.

**Спросить:** «Что взять для аватарок в комментариях?» Ответ: cover.

## aspect-ratio (2 мин)

**На слайде:** три блока с разными пропорциями.

**Рассказать:** без пропорций страница прыгает, пока грузятся картинки, — неудобно и мешает Lighthouse.

## srcset: браузер выбирает файл (3 мин)

**На слайде:** `img` с `srcset` и `sizes`.

**Рассказать:** телефону не нужно фото 1600px. Браузер сам выберет файл по ширине экрана и плотности пикселей.

## picture: другая картинка (2 мин)

**На слайде:** `picture` с двумя вариантами фото.

**Рассказать:** `picture` — когда нужна другая обрезка, а не просто другой размер.

## Сетка без медиазапросов (3 мин)

**На слайде:** auto-fill и minmax.

**Рассказать:** если карточки одинаковые — медиазапросы не нужны. Если по макету строго 1, 2 и 3 колонки — нужны.

## Часть 4. Переменные и темы (1 мин)

**На слайде:** разделитель четвёртой части.

**Рассказать:** последнее — как держать цвета в одном месте.

## CSS-переменные (4 мин)

**На слайде:** объявление и использование.

**Показать вживую:** песочница «Фирменный цвет в одном месте» — ученик меняет цвет, перекрашивается всё.

## Запасное значение (2 мин)

**На слайде:** `var()` с запасным цветом.

**Рассказать:** запасное значение спасает от опечатки в имени переменной.

## Темы (2 мин)

**На слайде:** одна карточка в светлой и тёмной теме.

**Рассказать:** тема — это набор других значений тех же переменных. CSS правил не меняется.

## Тема по настройке системы (3 мин)

**На слайде:** `prefers-color-scheme` и переменные.

**Показать вживую:** в DevTools: ⋮ → More tools → Rendering → Emulate CSS media feature prefers-color-scheme: dark.

## Частые ошибки (3 мин)

**На слайде:** четыре карточки ошибок.

**Рассказать:** про палец — кнопки не меньше 44 на 44px. Про `:hover` — на телефоне его нет.

## Не запрещайте масштабирование (1 мин)

**На слайде:** тезис про `user-scalable=no`.

**Рассказать:** в старых статьях это советуют. Не надо: это доступность.

## Воркшоп: адаптируем свою страницу (20 мин)

**На слайде:** шесть шагов: от проверки в режиме устройств до настоящего телефона.

**Подготовить заранее:** `catalog.html` с прошлого урока. Добавьте в него блок `div.promo` с `width: 600px` и картинку без `max-width` — будет что чинить. Телефон в той же Wi-Fi, что и Mac.

**Показать вживую:**
1. **Режим устройств.** Откройте страницу через Go Live → <kbd>⌥</kbd>+<kbd>⌘</kbd>+<kbd>I</kbd> → <kbd>⇧</kbd>+<kbd>⌘</kbd>+<kbd>M</kbd>.
   - Сверху Dimensions → iPhone SE. Потом Responsive и тяните правый край страницы — ширина видна в поле.
   - ⋮ справа на панели устройства → Show media queries — появятся цветные полоски, пока пустые.
2. **Что вылезает.** На ширине 375 внизу появилась горизонтальная прокрутка.
   - Кнопка со стрелкой (<kbd>⇧</kbd>+<kbd>⌘</kbd>+<kbd>C</kbd>) → наводите на блоки: у `.promo` ширина 600 — вот он.
   - В Styles поменяйте `width` на `max-width` — прокрутка пропала. Перенесите в файл.
3. **Сначала телефон.** У `.catalog` оставьте без медиазапроса `grid-template-columns: 1fr;` — одна колонка.
4. **Медиазапросы.** В конце файла:
   - `@media (min-width: 640px) { .catalog { grid-template-columns: repeat(2, 1fr); } }`
   - `@media (min-width: 1024px) { .catalog { grid-template-columns: repeat(3, 1fr); } }`
   - <kbd>⌘</kbd>+<kbd>S</kbd> → в режиме устройств появились полоски 640 и 1024. Щёлкните по полоске — ширина встанет ровно на брейкпоинт.
   - В Styles правила внутри медиазапроса подписаны `@media (min-width: 640px)` — видно, что сработало.
5. **Резиновые размеры.**
   - `img { max-width: 100%; height: auto; display: block; }` — картинка перестала вылезать.
   - `h1 { font-size: clamp(1.75rem, 5vw, 3rem); }` — тяните ширину: заголовок плавно меняется и упирается в границы.
   - Удалите в `head` строку `meta name="viewport"` → <kbd>⌘</kbd>+<kbd>R</kbd> в режиме iPhone: страница мелкая, медиазапросы не работают. Верните.
6. **Настоящий телефон** — по желанию.
   - VS Code → <kbd>⌘</kbd>+<kbd>,</kbd> → в поиске `liveServer host` → поставьте `0.0.0.0`. Остановите и снова нажмите Go Live.
   - macOS спросит «Разрешить входящие подключения?» → Разрешить.
   - Узнайте адрес Mac: Системные настройки → Wi-Fi → Подробнее → IP-адрес, например `192.168.1.5`. На телефоне откройте `http://192.168.1.5:5500/catalog.html`.

**Рассказать:** режим устройств — быстрая проверка, но финальная — на настоящем телефоне: там другой шрифт, палец вместо мыши и нет `:hover`.

## Домашнее задание (3 мин)

**На слайде:** четыре задания.

**Показать вживую:** открыть задание 1 в режиме устройств, поменять ширину — плашка перепроверяется сама и показывает текущую ширину.
